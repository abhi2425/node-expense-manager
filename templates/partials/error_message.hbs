<head>
    <style>
        .result {
            align-self: center;
            color: rgb(168, 18, 18);
            font-size: 2.8rem;
            font-family: monospace;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-align: center;
            width: 30rem;
        }
    </style>
</head>

<body>
    <h1 class="result">{{message}}</h1>
</body>